# ==-----------------------------------------------------------== #
# SPDX-FileCopyrightText: © 2025 Nayan Patil <nayantsg@proton.me>
#
# SPDX-License-Identifier: GPL-3.0-or-later
# ==-----------------------------------------------------------== #

# Do not modify!
# This file was generated from a template using the necronux internal pkl package.
# Any changes should be made to the template, not this file.

name: CI-Pkl
'on':
  pull_request:
    branches:
    - develop
    - release/**
    - release-sync/feature/**
    - release-sync/bugfix/**
    - release-sync/hotfix/**
    paths:
    - '*.pkl'
    - '**/*.pkl'
    - stdschema/**
    - tools/**
    - '*.kts'
    - '**/*.kts'
    - gradle/**
    - gradlew
    - gradlew.bat
    - justfile
  push:
    paths:
    - '*.pkl'
    - '**/*.pkl'
    - stdschema/**
    - tools/**
    - '*.kts'
    - '**/*.kts'
    - gradle/**
    - gradlew
    - gradlew.bat
    - justfile
  workflow_dispatch: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  conclusion:
    name: Conclusion
    if: ${{ !cancelled() }}
    needs:
    - resolvecheck
    - evalcheck
    - test
    permissions:
      contents: none
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Conclusion
      shell: bash
      run: |-
        jq -C <<< '${{ toJson(needs) }}'
        jq --exit-status 'all(.result == "success")' <<< '${{ toJson(needs) }}'
  resolvecheck:
    name: Resolve Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Install just
      uses: taiki-e/install-action@v2
      with:
        tool: just
    - name: Check if necronux.std.schema pkl project can be resolved
      shell: bash
      run: just resolve-stdschema
    - name: Check if necronux.internal.ci pkl project can be resolved
      shell: bash
      run: just resolve-internalci
  evalcheck:
    name: Eval Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Install just
      uses: taiki-e/install-action@v2
      with:
        tool: just
    - name: Check if necronux.std.schema pkl modules can be evaluated
      shell: bash
      run: just eval-stdschema
    - name: Check if necronux.internal.ci pkl modules can be evaluated
      shell: bash
      run: just eval-internalci
    - name: Verify pkl modules evaluation reflects the latest changes
      shell: bash
      run: git diff --exit-code
  test:
    name: Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    - name: Install just
      uses: taiki-e/install-action@v2
      with:
        tool: just
    - name: Test necronux.std.schema pkl modules
      shell: bash
      run: just test-stdschema
